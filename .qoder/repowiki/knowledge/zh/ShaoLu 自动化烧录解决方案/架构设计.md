根目录通过 ShaoLu.slnx 定义跨平台配置（Any CPU / x64），并聚合两个子项目：ShaoLu/ShaoLu.csproj（WPF 主应用）与 NUnitTest/NUnitTest.csproj（NUnit 4 测试）。主应用采用 MVVM + DI 容器组织 UI、步骤模型、OCR/图像编辑、用户与设置服务；测试项目针对主项目的核心执行链路（步骤、条件评估器、执行上下文、模型与 JSON 序列化）进行覆盖。两者共享同一套步骤模型与执行语义，测试作为主应用的契约验证层，形成「应用实现 + 单元测试」的双层结构。