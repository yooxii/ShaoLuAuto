<Window x:Class="ShaoLu.Views.WindowBurnInInput"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lex="http://wpflocalizeextension.codeplex.com"
        lex:ResxLocalizationProvider.DefaultAssembly="ShaoLu"
        lex:ResxLocalizationProvider.DefaultDictionary="Strings"
        Title="{lex:Loc BurnIn_Title}"
        Width="420"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="True"
        KeyDown="Window_KeyDown">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{lex:Loc BurnIn_InputHint}" TextWrapping="Wrap"
                   Foreground="Gray" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,8">
            <TextBlock Text="{lex:Loc BurnIn_OrderNo}" Margin="0,0,0,4"/>
            <TextBox x:Name="OrderNoBox" Height="28" FontSize="13" Padding="4,2"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,8">
            <TextBlock Text="{lex:Loc BurnIn_Operator}" Margin="0,0,0,4"/>
            <TextBox x:Name="OperatorBox" Height="28" FontSize="13" Padding="4,2"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,0,0,16">
            <TextBlock Text="{lex:Loc BurnIn_PartName}" Margin="0,0,0,4"/>
            <TextBox x:Name="PartNameBox" Height="28" FontSize="13" Padding="4,2"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="{lex:Loc OK}" MinWidth="80" Padding="12,4"
                    Margin="0,0,8,0" Click="OkButton_Click"/>
            <Button Content="{lex:Loc Cancel}" MinWidth="80" Padding="12,4"
                    Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
