# AutoShaoLu（自动化烧录）使用说明书

> 本说明书面向普通使用者，帮助你快速上手本程序的使用方法。

---

## 目录

1. [程序简介](#一程序简介)
2. [认识主界面](#二认识主界面)
3. [登录与编辑权限](#三登录与编辑权限)
4. [快速上手：5 分钟做出第一个自动化流程](#四快速上手)
5. [步骤编辑详解](#五步骤编辑详解)
6. [图片编辑窗口（裁剪与点击点）](#六图片编辑窗口)
7. [运行、暂停与停止](#七运行暂停与停止)
8. [保存与打开步骤文件](#八保存与打开步骤文件)
9. [步骤脚本文件格式（.autostep）](#九步骤脚本文件格式autostep)
10. [设置](#十设置)
11. [执行日志](#十一执行日志)
12. [用户管理](#十二用户管理)
13. [常见问题](#十三常见问题)

---

## 一、程序简介

AutoShaoLu 是一款桌面自动化工具。它可以像"机器人"一样替你操作电脑：

- **看图操作**：在屏幕上找到指定的图片（比如某个按钮），然后自动点击它；
- **自动输入**：自动键入文字，支持固定文本、递增编号、从文件逐行读取；
- **读取屏幕**：通过 OCR 文字识别或读取界面文本，获取屏幕上显示的内容；
- **流程控制**：支持条件跳转、循环、弹窗人工确认等，能组合出复杂的自动化流程。

典型使用场景：软件烧录、批量录入、重复性测试、跨系统搬运数据等。

---

## 二、认识主界面

启动程序后，主界面自上而下分为三部分：

`【图1：主界面整体截图，标注菜单栏、工具栏、步骤列表、步骤详情区】`

| 区域 | 位置 | 说明 |
|------|------|------|
| 菜单栏 | 最顶部 | 文件 / 设置 / 编辑 / 执行日志 / 烧录统计 / 语言 / 帮助 / 退出 |
| 工具栏 | 菜单下方 | 运行控制按钮（运行/暂停/停止）、编辑按钮、添加步骤按钮 |
| 步骤列表 | 左侧大区域 | 按行显示所有步骤，行号即执行顺序 |
| 步骤详情 | 右侧区域 | 选中某个步骤后，在此编辑它的具体参数 |

**菜单说明：**

| 菜单 | 功能 |
|------|------|
| 文件 | 新建、打开、保存、另存为步骤文件（快捷键 Ctrl+N / Ctrl+O / Ctrl+S / Ctrl+Shift+S） |
| 设置 | 打开设置窗口（主题、字体、默认参数等） |
| 编辑 | 登录 / 注销、用户管理 |
| 执行日志 | 查看文字输入类步骤的历史执行记录 |
| 烧录统计 | 查看烧录统计信息 |
| 语言 | 切换界面语言（English / 简体中文 / 繁體中文） |
| 帮助 | 示例、使用文档、控件试验场、关于 |

**帮助菜单说明：**

| 子项 | 功能 |
|------|------|
| 示例 | 列出程序自带 Examples 目录下的 `.autostep` 示例文件，点击即可打开 |
| 使用文档 | 打开本使用文档的 XPS 版本预览窗口 |
| 控件试验场 | 打开测试/教学窗口：左侧是各类靶子控件（按钮、文本框、下拉框等，按类别分行），右侧显示屏幕名称、刷新率、分辨率、鼠标位置；点击左侧任意控件，其类型、名称和内容会记录到右下方的信息框中，适合验证自动化步骤的定位效果 |
| 关于 | 版本与联系信息 |

---

## 三、登录与编辑权限

为了保护步骤脚本不被误改，程序将操作分为两类：

- **只读操作**（无需登录）：打开文件、查看步骤、运行流程；
- **编辑操作**（需要登录）：新建、保存、增删/复制/移动步骤、修改步骤参数。

**未登录时**，所有编辑相关的按钮、步骤列表和详情面板都是灰色只读状态。

**获取编辑权限：**

1. 点击菜单 **编辑 → 登录**；
2. 输入用户名和密码，点击"登录"。

`【图2：登录窗口截图，标注右上角关闭按钮和"立即注册"链接】`

**第一次使用（还没有任何账号）：**

1. 在登录窗口点击 **立即注册**；
2. 窗口会提示"首个注册的用户将自动成为管理员"；
3. 输入用户名和密码完成注册，无需任何人审批；
4. 返回登录界面，用刚注册的账号登录即可。

**之后注册的新用户**，需要已有管理员输入用户名和密码审批后才能注册成功。

> 忘记账号密码请联系管理员，管理员可在"用户管理"中重置密码。

---

## 四、快速上手

下面用一个最简单的例子，带你完整走一遍流程：**在屏幕上找到"确定"按钮并点击它**。

### 第 1 步：截图准备

先用系统截图工具（如 Win+Shift+S），把你要识别的目标（例如"确定"按钮）截图保存为 png 文件。

> 提示：截图要尽量只包含目标本身，不要带上周围的无关内容。

### 第 2 步：添加步骤

1. 登录获取编辑权限；
2. 在工具栏"步骤"区，点击 **点击图像** 按钮（或右键步骤列表 → 添加 → 点击图像）；
3. 步骤列表中出现一行新步骤，右侧详情面板显示它的参数。

`【图3：添加步骤后的界面，右侧显示"点击图像"步骤的参数面板】`

### 第 3 步：配置图片

1. 在右侧详情面板点击 **选择**，选中第 1 步准备的截图；
2. 点击 **编辑** 打开图片编辑窗口，裁剪出最精确的识别区域（详见[第六章](#六图片编辑窗口)）；
3. 设置相似度阈值（默认 0.85，一般不需要改）。

### 第 4 步：运行

点击工具栏 **运行**（或按快捷键 `Ctrl+Alt+F9`），程序就会在屏幕上寻找这张图片，找到后自动点击。

 恭喜，你已经完成了第一个自动化流程！接下来可以继续添加更多步骤，让它们按顺序执行。

---

## 五、步骤编辑详解

### 5.1 步骤的基本操作

在步骤列表上右键，或使用工具栏"编辑"区的按钮：

| 操作 | 说明 |
|------|------|
| 添加 | 在当前选中步骤的下方插入新步骤 |
| 删除 | 删除选中的步骤（支持多选） |
| 上移 / 下移 | 调整步骤执行顺序 |
| 复制 / 剪切 / 粘贴 | 支持多行批量操作 |
| 勾选/取消勾选 | 第一列复选框控制该步骤是否参与执行 |

`【图4：步骤列表右键菜单截图】`

### 5.2 所有步骤共有的属性

无论哪种步骤，都有以下通用属性（在步骤列表或详情面板中编辑）：

| 属性 | 说明 |
|------|------|
| 启用（复选框） | 取消勾选则运行时跳过该步骤 |
| 名称 | 步骤的名字，方便识别 |
| 描述 | 备注信息，记录这个步骤是干什么的 |
| 等待时间 | 执行本步骤前先等待的秒数 |
| 如果真 | 步骤结果为"真"时，跳转到哪个步骤执行（下拉选择） |
| 如果假 | 步骤结果为"假"时，跳转到哪个步骤执行（下拉选择） |
| 记录执行日志 | 勾选后，每次执行会记录输入内容和时间（见[执行日志](#十一执行日志)） |

**跳转（如果真 / 如果假）是实现循环和分支的关键：**

- 选择"(无跳转)"：执行完继续下一步；
- 选择某个步骤：执行完直接跳到那一行；
- 例：第 3 步"如果假"跳回第 1 步，就形成了一个循环，直到第 3 步结果为真才继续往下走。

`【图5：步骤详情面板中"如果真/如果假"下拉框的操作示例】`

> **自引用保护**：如果某步骤的跳转指向它自己（形成单步死循环），程序会在连续执行达到上限（默认 10 次，可在设置中调整）后自动跳过该步骤，防止卡死。

### 5.3 条件判断（进阶）

每个步骤的"结果真假"默认由它自身的执行情况决定（例如图片找到为真、没找到为假）。
如果需要更灵活的判断，可在详情面板切换到 **自定义条件** 模式，添加任意多条规则：

规则格式为：`变量` + `运算符` + `值`，多条规则之间用"并且 / 或者"连接。

- **变量**：可以是常量（恒真/恒假）、本步骤的执行结果（相似度、耗时、点击位置、OCR 文本、弹窗结果等），也可以引用**其他步骤**的执行结果；
- **运算符**：等于、不等于、大于、小于、包含、不包含、为空、不为空、正则匹配等；
- **OCR 文本**还支持提取处理：取指定行、从某关键字之后截取、取开头/末尾若干字符等。

例：`第2步的OCR文本 包含 "PASS"` → 为真时走"如果真"分支。

`【图6：自定义条件规则行的编辑界面】`

### 5.4 各步骤类型说明

#### ① 点击图像（ClickImage）

在屏幕上查找目标图片，找到后点击指定位置。

| 参数 | 说明 |
|------|------|
| 目标图片 | 「选择」选取图片，「编辑」打开图片编辑窗口裁剪并设置点击点 |
| 截图 | 全屏截图 / 窗口截图 / 矩形截图，截图后自动作为目标图片并打开图片编辑窗口（原裁剪与点击点被重置） |
| 相似度阈值 | 0~1，越高匹配越严格（建议 0.8~0.95） |
| 点击次数 / 点击间隔 | 找到后的点击次数与每次点击的间隔秒数 |
| 超时时间 | 超过该秒数仍未找到则判定失败 |

> 提示：点击图像步骤还带有“鼠标动作”列表（含标题栏：动作 / 次数 / 间隔时间(s)），可用多个动作替代默认单击，支持拖拽，见[⑧ 鼠标操作](#54-各步骤类型说明)。

#### ② 寻找图像（FindImage）

只查找、不点击。找到为真、找不到为假，常配合跳转做条件判断（例如"等待某个窗口出现再继续"）。

| 参数 | 说明 |
|------|------|
| 目标图片 / 相似度阈值 | 同上 |
| 重新寻找的间隔时间 | 没找到时，隔多久再找一次 |
| 超时时间 | 超过该秒数仍未找到则判定失败 |

#### ③ 键入文字（TypeText）

在当前焦点处模拟键盘输入一段文字。

| 参数 | 说明 |
|------|------|
| 输入内容 | 要输入的文字 |
| 按键间隔 | 每个按键之间的延迟秒数；设为 0 使用兼容性更好的安全输入模式 |

> 使用前请先确保输入焦点位于目标输入框（通常在本步骤前加一个"点击图像"步骤）。

#### ④ 键入文字（多）（TypeTextMore）

按"前缀 + 中缀 + 后缀"组合输入，并支持**自动递增**。适合批量输入有编号的内容。

例：前缀 `SN`（勾选递增）、中缀 `-`、后缀 `2024`，每轮循环输入的内容会变为 `SN1-2024`、`SN2-2024`……

| 参数 | 说明 |
|------|------|
| 前缀 / 中缀 / 后缀 | 三段文本片段 |
| 自增（各段独立） | 勾选后，每次执行该段自动 +1 |
| 新一轮重置计数 | 每轮循环开始时重置递增计数 |

#### ⑤ 键入文字（从文件）（TypeTextFromFile）

从 txt / csv / xlsx 文件读取内容清单，每次执行输入其中一条，逐条往下走。适合批量录入。

| 参数 | 说明 |
|------|------|
| 文件路径 | 选择数据文件；xlsx 读取第一个工作表的第一列 |
| 内容列表 | 加载后显示拆分出的所有条目，可手动增删 |
| 索引 | 当前读到第几条（可手动重置） |

#### ⑥ 弹出消息（Popup）

弹出对话框等待人工点击，根据点击的按钮决定结果真假。适合"人工确认""人工判断后选择走向"的场景。

| 参数 | 说明 |
|------|------|
| 标题 / 内容 | 弹窗显示的标题和正文 |
| 类型 | Information / Warning / Error / Question（影响图标） |
| 按钮组 | 自定义按钮文字，并指定每个按钮返回"真"还是"假" |
| 字体 / 颜色 | 自定义正文的字体和颜色 |

#### ⑦ 获取输入（GetInput）

从屏幕上"读取"信息，供后续条件判断使用。两种方式：

- **OCR 识别**：框选屏幕上一块区域，识别其中的文字；
- **读取界面文本**：指定屏幕上一个位置，读取该位置处控件的可选择文本（比 OCR 更准，但要求界面支持）。

读取到的文本可通过[自定义条件](#53-条件判断进阶)参与判断（如"OCR 文本包含 PASS"）。

`【图7：获取输入步骤的参数面板，含框选屏幕区域的按钮】`

#### ⑧ 鼠标操作（MouseAction）

不依赖图像识别，直接在指定位置执行鼠标动作。

| 参数 | 说明 |
|------|------|
| 位置模式 | 屏幕绝对坐标（可点“选择位置”在屏幕上点选）/ 鼠标当前位置 |
| 动作列表 | 带标题栏（动作 / 次数 / 间隔时间(s)），依次执行多个动作：左键单击/双击、右键、滚轮、拖拽等 |

**拖拽动作：**

动作类型选择 **Drag（拖拽）** 后，行末会出现 **编辑拖拽路径** 按钮。两种步骤各有一个专属编辑窗口：

**① 图像类步骤（点击图像等）——按点击点连线：**

- 窗口预览原图与所有点击点（两者均只读，不可改动）；
- 在某个点击点上**按住左键不松手**，拖向下一个点击点即可生成移动轨迹；最后一个点击点没有下一点，不可拖动；
- 鼠标进入“激活半径”（可设置）范围内时，点击点会放大表示可以连线；中途松手则直接从当前位置连线到下一个点击点；
- 未绘制轨迹的相邻点击点之间默认直线连接；运行时轨迹随匹配到的图像位置自动换算到屏幕坐标。

**② 鼠标操作步骤——全屏手绘轨迹：**

- 打开全屏采集窗口，**按住左键拖拽**即生成移动轨迹（屏幕绝对坐标）；
- 松手后窗口不退出，可再次按住左键**重新生成**轨迹；
- 点击“确认”保存退出，右键或 ESC 取消；“次数”为整条路径的重复次数。

#### ⑨ 统计信息（Statistics）

执行到此步骤时，打开一个**悬浮统计窗口**，实时显示运行数据，适合烧录时观察进度。

| 参数 | 说明 |
|------|------|
| 窗口标题 / 位置 | 悬浮窗的标题和屏幕位置 |
| 置顶 / 自动关闭 | 是否始终置顶；N 秒后自动关闭（0=不关闭） |
| 背景颜色 / 不透明度 | 自定义窗口外观 |
| 统计项 | 勾选要显示的内容：总执行时间、当前步骤、各步骤耗时/执行次数/执行结果等 |

`【图8：运行时的统计悬浮窗示例】`

#### ⑩ 窗口聚焦（FocusWindow）

把指定窗口放在最前方并激活（最小化时先恢复），常用于保证目标窗口可见、可操作。

| 参数 | 说明 |
|------|------|
| 指定窗口 | 点击“枚举窗口”列出当前所有可见窗口，从下拉列表中选择（精确匹配，优先使用） |
| 标题关键字 | 未选择窗口时，按标题包含匹配（不区分大小写）查找第一个命中窗口 |

> 未找到窗口时该步骤记为失败（结果为假），可配合“如果假”跳转做重试或跳过。

---

## 六、图片编辑窗口

识图类步骤（点击图像 / 寻找图像）点击"编辑"按钮后打开此窗口，用于精确设置**识别区域**和**点击位置**。

`【图9：图片编辑窗口整体截图，标注裁剪框、点击点标记、预览区、底部按钮】`

窗口分为三部分：

1. **上方菜单**：添加/删除点击点、全部显示/隐藏；
2. **中间主区**：原图 + 可拖动的裁剪框 + 红色点击点标记，下方是裁剪结果预览；
3. **底部按钮**：裁剪 / 保存 / 另存为 / 返回。

> **另存为**：把当前图片导出到指定位置（可选 png/jpg/bmp）。已裁剪时保存裁剪结果，未裁剪时保存原图；“保存”则是写回步骤供运行使用。

### 裁剪识别区域

1. 拖动裁剪框的边缘控制点调整大小，拖动整体移动位置；
2. 支持鼠标滚轮缩放视图、中键平移，方便精确定位；
3. 点击 **裁剪** 按钮确认，预览区会显示裁剪结果；
4. 点击 **保存** 写回步骤。

> 裁剪区域越小越精确，识别越快、越不易误匹配。建议只框选目标最有辨识度的部分。

### 设置点击点

找到图片后，鼠标点哪里由"点击点"决定：

| 操作 | 方法 |
|------|------|
| 添加 | 菜单 →「增加点击位置」，出现红色圆形标记 |
| 移动 | 直接拖拽标记到目标位置 |
| 查看坐标 | 鼠标悬停显示绝对坐标 |
| 重置 / 删除 / 隐藏 | 右键点击点操作 |

可以添加多个点击点，运行时会按顺序依次点击。未设置点击点时，默认点击图片中心。

> **注意**：复制一个识图步骤后，它的裁剪区域和点击点会一并复制；如需调整请打开副本的"编辑"窗口重新裁剪。

---

## 七、运行、暂停与停止

| 操作 | 按钮 | 快捷键 |
|------|------|--------|
| 运行 | 工具栏「运行」 | `Ctrl+Alt+F9` |
| 暂停 / 继续 | 工具栏「暂停」 | — |
| 停止 | 工具栏「停止」 | `Ctrl+Alt+F10` |

- 运行时主窗口会自动最小化（可在设置中关闭），结束后自动恢复；
- 步骤列表会高亮当前正在执行的步骤；执行出错的步骤行会标红，鼠标悬停可看错误原因；
- 若设置了"运行前确认"（见设置），点击运行后会先弹出确认框。

`【图10：运行中的界面，当前执行步骤高亮显示】`

---

## 八、保存与打开步骤文件

### 保存

1. 菜单 **文件 → 保存**，选择保存位置，文件扩展名为 `.autostep`；
2. **另存为**：把当前步骤保存为另一个文件（原文件不变）；
3. 保存时会把**所有裁剪后的识别图片一并打包**进文件，拷贝到任何电脑都能直接使用。

### 打开

1. 菜单 **文件 → 打开**，选择 `.autostep` 文件（也兼容旧版 `.json` 步骤文件）；
2. 程序会把压缩包中的图片解压到文件旁边的 `{文件名}_images` 文件夹中供运行使用；
3. 已安装本程序的电脑也可以**直接双击 `.autostep` 文件**打开。

> 注意：`{文件名}_images` 文件夹是运行时的工作目录，删除它会导致图片丢失（重新打开文件会自动重建）。

---

## 九、步骤脚本文件格式（.autostep）

了解文件格式有助于你手动检查、备份或版本管理步骤脚本。

### 9.1 整体结构

`.autostep` 文件本质上是一个 **zip 压缩包**（把扩展名改成 `.zip` 即可用解压软件打开查看），内部结构如下：

```
我的流程.autostep
├── steps.json                  ← 所有步骤的定义（核心文件）
└── images/                     ← 各识图步骤的裁剪图片
    ├── 3f2a1c8e-….png          ← 图片文件名 = 所属步骤的 Uid
    ├── 9b7d4e11-….png
    └── ...
```

- **steps.json**：UTF-8 编码的 JSON 数组，每个元素描述一个步骤；
- **images/**：只存放**裁剪后的识别图片**（不打包原始大图）。图片文件名为该步骤的唯一 Uid，与 steps.json 中的 `CroppedImageName` 字段对应；
- 步骤中引用的原图路径（`ImagePath`）仅作为文本记录保存在 JSON 中，不会打包。换电脑后如果原图路径失效，只需重新"选择"图片即可，不影响裁剪图的使用。

`【图11：用解压软件打开 .autostep 文件看到的内部结构截图】`

### 9.2 steps.json 内容说明

steps.json 是一个步骤数组，按数组顺序执行。每个步骤都包含一组通用字段 + 类型专属字段。

**通用字段：**

| 字段 | 含义 |
|------|------|
| `Type` | 步骤类型，如 `ClickImage`、`TypeText`、`Popup`、`GetInput`、`MouseAction`、`Statistics`、`FocusWindow` 等 |
| `Uid` | 步骤的唯一标识（GUID），跳转和条件引用都靠它 |
| `Name` / `Description` | 步骤名称 / 描述 |
| `IsNeed` | 是否启用 |
| `WaitTime` | 执行前等待秒数 |
| `TrueGoto` / `FalseGoto` | 如果真 / 如果假 跳转的目标步骤 **Uid**（为空表示不跳转） |
| `ConditionMode` | 条件模式：`Default`（默认，用自身结果）/ `Custom`（自定义规则） |
| `Conditions` | 自定义条件规则行列表 |
| `EnableLog` | 是否记录执行日志 |
| `SelfReferenceLimit` | 自引用上限（-1=不限，0=禁止，>0=次数） |

**示例（一个"点击图像"+一个"键入文字"组成的脚本，已简化）：**

```json
[
  {
    "Type": "ClickImage",
    "Uid": "3f2a1c8e-1234-5678-9abc-def012345678",
    "Name": "点击确定按钮",
    "IsNeed": true,
    "WaitTime": 0.1,
    "TrueGoto": null,
    "FalseGoto": null,
    "ImagePath": "D:\\Screenshots\\ok_button.png",
    "CroppedImageName": "images/3f2a1c8e-1234-5678-9abc-def012345678.png",
    "SimilarityThreshold": 0.85,
    "Clicks": 1,
    "ClickGap": 0.1,
    "Timeout": 3
  },
  {
    "Type": "TypeText",
    "Uid": "9b7d4e11-aaaa-bbbb-cccc-ddddeeeeffff",
    "Name": "输入序列号",
    "IsNeed": true,
    "WaitTime": 0.5,
    "TrueGoto": null,
    "FalseGoto": null,
    "TextToType": "SN20240101",
    "EnableLog": true
  }
]
```

`【图12：steps.json 用记事本/编辑器打开后的截图】`

**说明：**

- 手动编辑 steps.json 是可行的（例如批量改名称、改等待时间），但修改前请先备份，JSON 语法错误会导致文件无法打开；
- 旧版脚本中 `TrueGoto`/`FalseGoto` 若为数字行号，打开时会自动转换为 Uid，无需手动处理；
- 不要手动修改 `Uid`、`TrueGoto`、`FalseGoto` 之间的引用关系，否则跳转和条件会失效。

---

## 十、设置

菜单 **设置** 打开设置窗口，左侧树切换分类，修改后点击"保存"生效。

`【图13：设置窗口截图】`

**App 设置：**

| 选项 | 说明 |
|------|------|
| 主题 | 亮色 / 暗色 |
| 字体 / 字号 | 主界面全局字体 |
| 日志保留天数 | 执行日志自动清理周期，0 = 永不清理 |

**Step 设置：**

| 选项 | 说明 |
|------|------|
| 展示错误弹窗 | 步骤执行出错时是否弹窗提示 |
| 运行时最小化窗口 | 点击运行后主窗口自动最小化 |
| 运行前确认 | 每次运行前弹出确认对话框，防误触 |
| 自引用上限 | 步骤跳向自身时的最大连续执行次数 |
| 新建步骤默认参数 | 新建步骤时的默认相似度阈值、等待时间、超时时间、点击次数 |

---

## 十一、执行日志

文字输入类步骤勾选"记录执行日志"后，每次成功执行都会记录：**步骤 Uid、步骤文件名、步骤名称、实际输入内容、执行时间**。

菜单 **执行日志** 打开查询窗口：

- 支持按**关键字**搜索（匹配输入内容和步骤名称）；
- 支持按**步骤文件名、时间范围**筛选；
- 点击列头可排序，底部支持翻页。

`【图14：执行日志查询窗口截图，展示搜索框、筛选条件和结果列表】`

---

## 十二、用户管理

管理员登录后，菜单 **编辑 → 用户管理** 打开管理窗口，可以：

- 查看所有用户（用户名、角色、创建时间）；
- 添加用户（可选择"管理员"或"普通用户"角色）；
- 为选中用户修改密码；
- 删除用户（最后一个管理员账户不可删除，自己不能删除自己）。

`【图15：用户管理窗口截图】`

> 管理员与普通用户的区别：只有管理员能打开用户管理窗口。编辑步骤的权限只需登录即可，与角色无关。

---

## 十三、常见问题

**Q1：找不到图片 / 经常误识别？**
- 重新"编辑"裁剪，让识别区域更小更精确；
- 调低或调高相似度阈值（0.8~0.95 之间尝试）；
- 确认屏幕分辨率、缩放比例与截图时一致。

**Q2：输入文字不完整或乱码？**
- 增大按键间隔（如 0.05~0.1 秒）；
- 或将按键间隔设为 0 使用安全输入模式；
- 确认输入前焦点已在目标输入框。

**Q3：换电脑后打开 .autostep 提示图片错误？**
- 裁剪图已打包在文件内，通常不会出现此问题；
- 若原图路径提示错误，只影响重新编辑，不影响运行；删除 `{文件名}_images` 文件夹后重新打开文件即可重建。

**Q4：步骤陷入死循环停不下来？**
- 按 `Ctrl+Alt+F10` 强制停止；
- 检查跳转设置，必要时为自引用步骤设置上限。

**Q5：忘记管理员密码怎么办？**
- 用户数据保存在本机 `%AppData%\AutoShaoLu\users.db`，联系 IT 管理员处理。

---

*如遇到本说明书未涵盖的问题，请联系程序作者（菜单"关于"中有联系方式）。*
