# 修改记录

## 2026-09-11

### 类型：优化（弹窗界面 / 关闭条件 / 条件编辑窗口 / 复制粘贴 / 撤销恢复）

#### 需求描述
1. 弹窗步骤的紧凑模式更紧凑，窗口大小适应内容或标题。
2. 弹窗关闭条件支持多选组合（点击按钮 / 超时 / 到达步骤，任一满足即关闭）。
3. 每个步骤的结果判断编辑区域移到新窗口，原位置只保留入口。
4. 排查并修复步骤复制/粘贴问题。
5. 增加撤销/恢复：步骤列表级（增删等）与步骤内部级（每步独立）。

#### 修改内容说明
- 弹窗紧凑模式：进一步缩小边距、图标、按钮与关闭按钮尺寸；文档去掉段落外边距并禁用多列测量，窗口 SizeToContent 在紧凑模式下最小尺寸归零，使窗口按内容/标题自适应。
- 关闭条件改为 [Flags] PopupCloseMode（None/ButtonClick/Timeout/StepReached），步骤详情用三个复选框组合配置；RunAsync 按组合等待「按钮 / 超时 / 到达步骤」任一条件，StepReached 时可继续执行并支持后台超时自动关闭；旧版数字枚举通过 PopupCloseModeJsonConverter 自动迁移。
- 新增 WindowEditConditions 窗口承载原 ConditionPanelTemplate；步骤详情用 ConditionEntryTemplate 保留「编辑结果判断」入口和摘要（默认 / 自定义 N 条），摘要随条件增删与模式切换刷新。
- 复制/粘贴：统一深拷贝补齐各步骤 Clone() 未复制的基类状态（ConditionMode、Conditions、IsSave 等，条件项深拷贝）；粘贴始终基于剪贴板重新拷贝，保证多次粘贴互相独立、Uid 唯一；Cut 需登录且复制成功才删除；剪贴板为空时禁用粘贴；步骤列表新增 Ctrl+C/X/V 快捷键。
- 撤销/恢复：
  - 列表级：在新增、删除、上移、下移、粘贴、剪切前记录整表快照（深拷贝并保留 Uid，保证跳转/条件引用有效），最多 50 步；新建或打开步骤文件时清空历史。
  - 步骤内部级：以当前选中步骤为会话，监听其 PropertyChanged 与 Conditions 集合/条目变化，记录最多 100 步快照；切换到其他步骤时自动清空；恢复时通过反射把快照状态写回原实例，保持实例与 Uid 不变。
  - 工具栏与右键菜单新增撤销/恢复、撤销/恢复本步骤编辑入口，步骤列表支持 Ctrl+Z / Ctrl+Y / Ctrl+Shift+Z。
- 本地化：新增 CloseMode_None、CloseMode_Hint、Condition_EditConditions、Condition_SummaryDefault、Condition_SummaryCustom、Undo、Redo、UndoStepEdit、RedoStepEdit 共 9 个字符串（默认/简中/繁中/英文），并同步 Strings.Designer.cs。

#### 涉及文件
- ShaoLu/Views/WindowAsyncPopup.xaml、WindowAsyncPopup.xaml.cs
- ShaoLu/Models/AutomationStepModel.cs
- ShaoLu/Converters/PopupCloseModeJsonConverter.cs（新增）、ConditionEnumToStringConverter.cs
- ShaoLu/Viewmodels/AutomationStep.cs
- ShaoLu/Viewmodels/StepsViewModel.cs
- ShaoLu/Views/WindowEditConditions.xaml、WindowEditConditions.xaml.cs（新增）
- ShaoLu/Views/UserControlSteps.xaml
- ShaoLu/Templates/StepDetailTemplates.xaml
- ShaoLu/MainWindow.xaml.cs
- ShaoLu/ShaoLu.csproj
- ShaoLu/Resources/Strings.resx、Strings.zh-CN.resx、Strings.zh-TW.resx、Strings.en-US.resx、Strings.Designer.cs
- NUnitTest/AutomationStepTests.cs、ConverterTests.cs、StepsFileTests.cs

#### 构建验证结果
- 主项目（ShaoLu.csproj, Debug|x64）编译通过，无错误。
- 测试项目（NUnitTest.csproj, Debug）编译通过，无错误。
- NUnit 测试全部通过：189 / 189。

## 2026-09-11

### 类型：优化（获取输入步骤增加超时）

#### 需求描述
获取输入步骤执行耗时很长，需要查明原因并增加超时时间；新增文案需国际化。

#### 原因分析
- 读取界面文本方式通过 UI Automation 读取，`AutomationElement.FromPoint`、`TextPattern.DocumentRange.GetText(-1)` 等均为同步跨进程调用，目标程序繁忙或无响应时会长时间阻塞；图像相对定位的每个获取点还会执行屏幕模板匹配（原硬编码 3s），获取点较多时耗时叠加。
- OCR 识别方式首次调用需懒加载 Tesseract（chi_sim+eng）引擎，且识别区域越大耗时越长。
- 原 `RunAsync` 对上述操作没有任何超时保护，单步阻塞会拖住整个流程。

#### 修改内容说明
- `GetInputStep` 新增 `Timeout` 属性（秒，默认 3，小于等于 0 表示不限制），并纳入 `Clone`。
- `RunAsync` 通过 `RunWithTimeoutAsync` 对读取界面文本 / OCR 识别施加超时；超时抛出带本地化消息的 `TimeoutException`，由执行引擎映射为 `ExecutionTimeout`，外部取消仍保持取消语义。
- OCR 引擎访问加锁串行化，避免超时被放弃的识别任务与后续识别并发使用同一 Tesseract 实例。
- 相对定位单点图像匹配的超时可跟随步骤配置（小于等于 0 时保持原 3s）。
- 步骤详情模板新增超时时间输入框；新建步骤沿用设置中的默认超时时间。
- 新增 `GetInput_Timeout` 本地化字符串（默认/简中/繁中/英文），并在 `Strings.Designer.cs` 生成强类型属性。

#### 涉及文件
- ShaoLu/Viewmodels/GetInputStep.cs
- ShaoLu/Services/OCRService.cs
- ShaoLu/Viewmodels/StepsViewModel.cs
- ShaoLu/Templates/StepDetailTemplates.xaml
- ShaoLu/Resources/Strings.resx
- ShaoLu/Resources/Strings.zh-CN.resx
- ShaoLu/Resources/Strings.zh-TW.resx
- ShaoLu/Resources/Strings.en-US.resx
- ShaoLu/Resources/Strings.Designer.cs
- NUnitTest/AutomationStepTests.cs
- NUnitTest/StepsFileTests.cs

#### 构建验证结果
- 主项目（ShaoLu.csproj, Debug|x64）编译通过，无错误。
- 测试项目（NUnitTest.csproj, Debug）编译通过，无错误。
- NUnit 测试全部通过：183 / 183。

## 2026-08-12

### 类型：新增

#### 需求描述
主窗口菜单栏新增一级菜单"帮助"，将原有"关于"菜单项移入其中，并新增"示例"与"使用文档"两个子菜单：
1. 示例：子项从程序根目录的 `Examples` 文件夹动态读取所有 `.autostep` 文件，点击即打开该示例。该文件夹需随发布自带，示例文件由用户后续创建。
2. 使用文档：打开一个窗口预览 `Docs` 目录下的 `.xps` 类型文档。

#### 修改内容说明
- 主窗口菜单新增"帮助"一级菜单，包含"示例"、"使用文档"、"关于"子项（关于保持原有逻辑）。
- 将 `Open_Click` 中加载步骤文件的核心逻辑提取为 `LoadStepFile(string)` 私有方法，供"打开"与"示例"复用。
- 新增 `BuildExamplesMenu()`：窗口加载时扫描 `AppContext.BaseDirectory/Examples` 下的 `.autostep` 文件并动态生成菜单项；目录不存在或无文件时显示禁用的"(暂无示例)"占位项。
- 新增 `Example_Click`：从菜单项 `Tag` 取文件路径并调用 `LoadStepFile` 打开。
- 新增 `UserDoc_Click`：查找 `AppContext.BaseDirectory/Docs` 下第一个 `.xps` 文件并用文档预览窗口打开；未找到时弹出提示。
- 新增 XPS 文档预览窗口 `WindowDocumentViewer`，使用 WPF `DocumentViewer` 承载 `XpsDocument`，窗口关闭时释放文件句柄。
- csproj 新增 `ReachFramework`、`System.Printing` 程序集引用；新增 `WindowDocumentViewer` 的 Page/Compile 项；新增 `Examples\*.autostep` 与 `Docs\*.xps` 通配符 Content 包含（随构建/发布自动部署，用户后续新增文件无需改项目文件）。
- 创建 `ShaoLu/Examples/` 与 `ShaoLu/Docs/` 目录（含 README 占位说明）。
- 4 个本地化资源文件新增 `Help_Header`、`Examples_Header`、`UserDoc_Header`、`NoExamples`、`UserDocNotFound` 字符串（中文简/繁、英文、默认）。

#### 涉及文件
- ShaoLu/MainWindow.xaml
- ShaoLu/MainWindow.xaml.cs
- ShaoLu/Views/WindowDocumentViewer.xaml（新增）
- ShaoLu/Views/WindowDocumentViewer.xaml.cs（新增）
- ShaoLu/ShaoLu.csproj
- ShaoLu/Resources/Strings.resx
- ShaoLu/Resources/Strings.zh-CN.resx
- ShaoLu/Resources/Strings.zh-TW.resx
- ShaoLu/Resources/Strings.en-US.resx
- ShaoLu/Examples/README.txt（新增）
- ShaoLu/Docs/README.txt（新增）

#### 构建验证结果
- 主项目（ShaoLu.csproj, Debug|x64）编译通过，无错误。
- 测试项目（NUnitTest.csproj, Debug）编译通过，无错误。
- NUnit 测试全部通过：170 / 170。

## 2026-08-14

### 类型：新增 / 优化

#### 需求描述
1. 鼠标动作列表添加标题栏：动作 | 次数 | 间隔时间(s) |（预留空白，给编辑按钮留位）。
2. 鼠标动作新增拖拽（Drag）功能：选中拖拽后行末出现“编辑拖拽路径”按钮，打开浮窗编辑路径点集合（可添加/删除/排序）；路径点可以是图片编辑窗口中设置的点击位置，也可以是自定义的屏幕绝对坐标。
3. 帮助菜单新增用于测试和教学的“控件试验场”子项：左侧为按类别分行的靶子控件，右侧固定显示屏幕名称/刷新率/分辨率/鼠标位置，下方滚动信息框记录每个被点击控件的信息。
4. 新增“窗口置顶”步骤：将指定窗口置顶，支持枚举窗口选择 + 标题关键字匹配，动作为置顶/取消置顶/切换。
5. 同步更新根目录使用说明书。

#### 修改内容说明
- `MouseActionType` 新增 `Drag`；新增 `DragPointSource`（ImageClick/Absolute）与 `DragPathPoint` 模型；`MouseActionItem` 新增 `DragPath` 集合并纳入 Clone。
- `Autogui`：提取 `ComputeScreenTarget` 坐标计算方法；`ExecuteMouseActions` 新增拖拽路径点解析回调参数；新增 `ExecuteDrag`（首点按下左键→途经各点→末点释放）与图像步骤的 `ResolveImageDragPoint`（点击位置序号随匹配区域换算屏幕坐标，绝对坐标按 DPI 转物理像素）。
- `MouseActionStep`、`ClickImageStep` 新增 `EditDragPathCommand`，打开 `WindowDragPathEditor` 浮窗（图像步骤传入点击位置数量，鼠标操作步骤仅支持绝对坐标）。
- 新增 `WindowDragPathEditor`：路径点列表带序号/来源/点击位置序号/X/Y 列，支持添加、删除、上移/下移、全屏点选坐标；无图像点击位置时来源仅提供绝对坐标。
- 动作列表模板（点击图像与鼠标操作两处）新增标题栏行；动作行末新增仅 Drag 时显示的“编辑拖拽路径”按钮。
- 新增 `TopmostWindowStep`（StepType.TopmostWindow = 204）：枚举窗口下拉选择（精确匹配优先）+ 标题关键字包含匹配；置顶/取消/切换三种动作；未找到窗口记为失败。`NativeMethods` 新增 EnumWindows/GetWindowText/IsWindowVisible/SetWindowPos/GetWindowLong 及显示模式相关 P/Invoke；新增 `WindowServices` 工具类。
- 新增 `WindowPlayground`（控件试验场）：左侧按钮/文本/选择/其他四类靶子控件，右侧屏幕信息与点击记录（DispatcherTimer 刷新鼠标位置，窗口关闭时停止）；帮助菜单新增入口。
- 步骤注册：AutomationStepJsonConverter 映射、StepsViewModel.AddStep 分支、UserControlSteps 两处菜单、StepTemplateSelector 与详情模板。
- 4 个 resx 新增 29 个资源键（动作列表标题、拖拽路径编辑、窗口置顶、控件试验场）。
- 单元测试新增 MouseActionItem/DragPathPoint 与 TopmostWindowStep 共 7 个用例。
- 更新根目录《使用说明书》：帮助菜单说明、文件快捷键、鼠标操作拖拽说明、新增⑩窗口置顶章节。

#### 涉及文件
- ShaoLu/Models/MouseActionItem.cs
- ShaoLu/Models/AutomationStepModel.cs
- ShaoLu/Utils/Autogui.cs
- ShaoLu/Utils/NativeMethods.cs
- ShaoLu/Utils/WindowServices.cs（新增）
- ShaoLu/Viewmodels/MouseActionStep.cs
- ShaoLu/Viewmodels/ImageRecognition.cs
- ShaoLu/Viewmodels/TopmostWindowStep.cs（新增）
- ShaoLu/Viewmodels/StepsViewModel.cs
- ShaoLu/Views/WindowDragPathEditor.xaml / .xaml.cs（新增）
- ShaoLu/Views/WindowPlayground.xaml / .xaml.cs（新增）
- ShaoLu/Views/UserControlSteps.xaml
- ShaoLu/Templates/StepDetailTemplates.xaml
- ShaoLu/Converters/StepTemplateSelector.cs
- ShaoLu/Converters/AutomationStepJsonConverter.cs
- ShaoLu/MainWindow.xaml / .xaml.cs
- ShaoLu/ShaoLu.csproj
- ShaoLu/Resources/Strings.resx、Strings.zh-CN.resx、Strings.zh-TW.resx、Strings.en-US.resx
- NUnitTest/AutomationStepTests.cs
- 使用说明书.md

#### 构建验证结果
- 主项目（ShaoLu.csproj, Debug|x64）编译通过，无错误。
- 测试项目（NUnitTest.csproj, Debug）编译通过，无错误。
- NUnit 测试全部通过：177 / 177。

### 类型：修复

#### 问题现象
新增点击图像步骤后无限弹出报错（日志：无法设置 System.Windows.Controls.ItemCollection 类型的值，StepDetailTemplates.xaml 第 247 行）。

#### 原因与修复
- 原因：动作列表标题栏 StackPanel 被放在了绑定 `ItemsSource="{Binding MouseActions}"` 的 ItemsControl 内部作为子元素；WPF 要求使用 ItemsSource 时 Items 集合必须为空，模板加载即抛异常并被全局异常兑底反复弹窗。
- 修复：将标题栏 StackPanel 移到 ItemsControl 外部（点击图像与鼠标操作两处模板），作为同级元素置于 GroupBox 内的 StackPanel 中，显示效果不变。

#### 涉及文件
- ShaoLu/Templates/StepDetailTemplates.xaml

#### 构建验证结果
- 主项目（ShaoLu.csproj, Debug|x64）编译通过，无错误。

### 类型：修复（国际化）

#### 问题现象
资源键 `Filter`（执行日志筛选按钮）与 `Msg_DeleteReferenced`（删除引用提示）仅存在于简体中文资源，其他语言缺失。

#### 修复内容
在默认英文、en-US、zh-TW 三份资源文件中补齐上述两个键。

### 类型：重构（拖拽路径编辑）

#### 需求描述
将两种拖拽按路径点类型分离，各自重做编辑窗口：
1. 点击点类型：预览原图与点击点（只读），按住左键从某点击点拖向下一点生成移动轨迹，末点不可拖；激活半径内点击点放大提示，中途松手直接从当前位置连线到下一点。
2. 绝对位置类型：在点选基础上改为全屏按住左键拖拽生成轨迹，松手不退出，再次按住重新生成，点确认才退出。

#### 修改内容说明
- 模型分离：删除 `DragPointSource`/`DragPathPoint`；`MouseActionItem.DragPath` 改为 `ObservableCollection<Point>`（绝对坐标序列）；新增 `ClickDragTrajectories`（相邻点击点间手绘轨迹，相对裁剪区左上角偏移）。
- `Autogui.ExecuteMouseActions` 回调改为返回完整路径物理坐标序列；图像步骤新增 `BuildImageDragPath`（点击点依次 + 相邻段轨迹插入）；删除逐点解析 `ResolveImageDragPoint`。
- 新增 `WindowDragPathClick`：原图+点击点预览（只读）、按住左键绘轨迹、激活半径（可调）内点击点放大、中途松手自动连线到下一点击点、轨迹可整体清除；替代原 `WindowDragPathEditor`。
- 新增 `WindowDragPathAbsolute`：全屏手绘轨迹采集，松手不退出、再次按住重新生成、确认/取消/右键/ESC；鼠标操作步骤接入（取消不改动）。
- 图像步骤 `EditDragPath` 接入新窗口（传入原图、点击点、裁剪区）；删除旧 `WindowDragPathEditor` 及 csproj 条目；替换旧 DragPath_* 资源键为 DragPathClick_*/DragPathAbs_*（4 语言）。
- 单元测试同步重写（新模型克隆独立性）；更新使用说明书拖拽章节。

#### 涉及文件
- ShaoLu/Models/MouseActionItem.cs
- ShaoLu/Utils/Autogui.cs
- ShaoLu/Viewmodels/ImageRecognition.cs
- ShaoLu/Viewmodels/MouseActionStep.cs
- ShaoLu/Views/WindowDragPathClick.xaml / .xaml.cs（新增）
- ShaoLu/Views/WindowDragPathAbsolute.xaml / .xaml.cs（新增）
- ShaoLu/Views/WindowDragPathEditor.xaml / .xaml.cs（删除）
- ShaoLu/ShaoLu.csproj
- ShaoLu/Resources/Strings.resx、Strings.zh-CN.resx、Strings.zh-TW.resx、Strings.en-US.resx
- NUnitTest/AutomationStepTests.cs
- 使用说明书.md

#### 构建验证结果
- 主项目（ShaoLu.csproj, Debug|x64）编译通过，无错误。
- NUnit 测试全部通过：176 / 176。

### 类型：修复（构建）

#### 问题现象
VS 编译报“未知的生成错误：未能找到文件 WindowDragPathEditor.xaml”。

#### 原因与修复
旧窗口删除后 obj\Debug 下残留其 IntelliSense 生成文件（.g.i.cs），已删除残留文件，AnyCPU/x64 均构建通过。

### 类型：新增（截图与另存为）

#### 需求描述
图片类步骤增加截图功能（全屏/窗口/矩形），截图后打开图片编辑窗口；图片编辑窗口增加另存为（有裁剪保存裁剪，否则保存原图）。

#### 修改内容说明
- `ImageRecognitionBase` 新增 `CaptureFullScreenCommand`/`CaptureWindowCommand`/`CaptureRegionCommand`：截图保存为工作目录 `images/{Uid}_src.png` 作为新原图，重置裁剪/点击点状态后自动打开图片编辑窗口。
- 新增 `WindowSelectWindowTitle` 窗口选择对话框（枚举可见窗口供选择，可刷新）；`NativeMethods` 新增 GetWindowRect/SetForegroundWindow/ShowWindow/IsIconic 等声明；窗口截图前先将目标窗口前置。
- `WindowEditImage` 底部新增“另存为”按钮：有裁剪结果保存裁剪图，否则保存原图，支持 png/jpg/bmp。
- 点击图像与寻找图像模板均新增截图按钮行。

### 类型：重构（窗口置顶→窗口聚焦、工具栏分组）

#### 需求描述
原“窗口置顶”步骤实为误加，应为“窗口聚焦”（把指定窗口放在最前方，不设置顶）；工具栏中文字类步骤与鼠标操作并入图片类分组。

#### 修改内容说明
- 删除 `TopmostWindowStep` 及置顶相关 API（SetWindowPos/GetWindowLong 等）；新增 `FocusWindowStep`（StepType.FocusWindow = 204）：查找窗口后置前并激活，最小化时先恢复；未找到记为失败。
- `WindowServices` 新增 `FocusWindow`，移除 `SetTopmost`/`IsTopmost`。
- 工具栏：移除“键入文字”SplitButton，其三个子项与鼠标操作移入“点击图像”SplitButton；“获取输入”分组保留获取输入/统计/烧录配置/窗口聚焦；处理器 `MenuItemAddTextStep_Click` 重命名为 `MenuItemAddOtherStep_Click`。
- resx 四语言：移除 AddTopmostWindowStep/Topmost_* 键，新增 AddFocusWindowStep/Focus_*/Capture* 键；同步更新序列化映射、模板选择器、详情模板、右键菜单与单元测试。
- 更新使用说明书（截图/另存为/窗口聚焦章节）。

#### 涉及文件
- ShaoLu/Viewmodels/ImageRecognition.cs
- ShaoLu/Viewmodels/FocusWindowStep.cs（新增）、TopmostWindowStep.cs（删除）
- ShaoLu/Views/WindowSelectWindowTitle.xaml / .xaml.cs（新增）
- ShaoLu/Views/WindowEditImage.xaml / .xaml.cs
- ShaoLu/Views/UserControlSteps.xaml / .xaml.cs
- ShaoLu/Templates/StepDetailTemplates.xaml
- ShaoLu/Converters/StepTemplateSelector.cs、AutomationStepJsonConverter.cs
- ShaoLu/Models/AutomationStepModel.cs
- ShaoLu/Utils/NativeMethods.cs、WindowServices.cs
- ShaoLu/Viewmodels/StepsViewModel.cs
- ShaoLu/ShaoLu.csproj
- ShaoLu/Resources/Strings.resx、Strings.zh-CN.resx、Strings.zh-TW.resx、Strings.en-US.resx
- NUnitTest/AutomationStepTests.cs
- 使用说明书.md

#### 构建验证结果
- 主项目（ShaoLu.csproj, Debug|x64）编译通过，无错误。
- NUnit 测试全部通过：176 / 176。

## 2026-08-17

### 类型：修复

#### 问题现象
二次全屏截图或窗口截图后，打开的图片编辑窗口仍显示第一张截图，图片不更新。

#### 原因与修复
- 原因：每次截图均覆写同一文件（工作目录 `images/{Uid}_src.png`），而 `BitmapImage` 通过 `UriSource` 加载时按 URI 缓存，同路径文件更新后仍返回旧图像。
- 修复：`ImageRecognitionBase.LoadImage` 与 `BurnInConfigStep.LoadFrozenBitmap` 改为 `StreamSource` + `FileStream` 流式加载（CacheOption.OnLoad），每次读取最新文件内容且不锁文件。

#### 构建验证结果
- 主项目（ShaoLu.csproj, Debug|x64）编译通过，无错误。
- NUnit 测试全部通过：176 / 176。

### 类型：新增 / 优化（鼠标长按与动作翻译、空步骤）

#### 需求描述
1. 鼠标动作新增“长按”，并将所有动作类型翻译为多语言显示；动作列表移除标题栏，改为悬停提示。
2. 启用空步骤类型：用作分割符/占位符/注释。

#### 修改内容说明
- `MouseActionType` 新增 `LeftPress`：执行时按下左键保持“间隔时间”后释放；`Autogui.ExecuteMouseActions` 新增对应分支。
- 动作类型显示国际化：`ConditionEnumToStringConverter` 新增 `ConvertMouseActionType`（MouseAction_* 资源键），两处动作列表 ComboBox 改用 ItemTemplate 显示翻译文本。
- 动作列表移除标题栏行，各列改为 ToolTip 悬停提示（动作类型/次数/间隔时间），删除 MouseActionHeader_* 资源键。
- 启用空步骤：`StepsViewModel.AddStep` 新增 Empty 分支；右键菜单与工具栏新增“添加空步骤”；`StepTemplateSelector` 新增 `EmptyStepTemplate` 属性与映射；详情模板含说明、注释框（绑定描述）、等待时间与跳转面板；执行始终成功且不进行任何操作（EmptyStep 原有实现）。
- resx 四语言新增 MouseActionTypeHint/MouseAction_*/AddEmptyStep/EmptyStep_Hint/EmptyStep_Comment 键。
- 更新使用说明书（鼠标操作动作说明、新增⑪空步骤章节）。

#### 涉及文件
- ShaoLu/Models/MouseActionItem.cs
- ShaoLu/Utils/Autogui.cs
- ShaoLu/Converters/ConditionEnumToStringConverter.cs、StepTemplateSelector.cs
- ShaoLu/Templates/StepDetailTemplates.xaml
- ShaoLu/Views/UserControlSteps.xaml
- ShaoLu/Viewmodels/StepsViewModel.cs
- ShaoLu/Resources/Strings.resx、Strings.zh-CN.resx、Strings.zh-TW.resx、Strings.en-US.resx
- 使用说明书.md

#### 构建验证结果
- 主项目（ShaoLu.csproj, Debug|x64）编译通过，无错误。
- NUnit 测试全部通过：176 / 176。

### 类型：修复 / 优化（菜单空标题与文案）

#### 问题现象与修复
1. “执行日志”“烧录统计”两个菜单标题显示为空：无键 `{lex:Loc}` 按 `x:Name_Header` 解析，而实际键名为 `ExecutionLog`/`BurnInStatistics`（无 _Header 后缀）。修复：MainWindow.xaml 改为显式键 `{lex:Loc ExecutionLog}`/`{lex:Loc BurnInStatistics}`。
2. 菜单项“添加空步骤”去掉“添加”二字，改为“空步骤”；今后新增步骤类型菜单名不再带“添加/Add”前缀。
3. “长按”动作文案补齐为“左键长按”（含动作类型悬停提示，4 语言）。

#### 涉及文件
- ShaoLu/MainWindow.xaml
- ShaoLu/Resources/Strings.resx、Strings.zh-CN.resx、Strings.zh-TW.resx、Strings.en-US.resx

#### 构建验证结果
- 主项目（ShaoLu.csproj, Debug|x64）编译通过，无错误。

### 类型：新增 / 优化（使用文档按 OS 版本选择查看方式）

#### 需求描述
Docs 目录提供 XPS 与 PDF 两份使用文档；应用启动时检测 OS 版本，Win7 用户用 XPS/DocumentViewer 浏览，Win10+ 用户用 WebView2 浏览 PDF。

#### 修改内容说明
- 新增 `OsVersionHelper`（RtlGetVersion 精确获取真实版本号，规避 .NET Framework 的 Environment.OSVersion 兼容性 shim）：`IsWindows10OrLater` 与 `VersionText`；App 启动时记录 OS 版本日志。
- `WindowDocumentViewer` 恢复为 XPS + DocumentViewer 实现（原 WebView2 打开 xps 不可靠），关闭时释放 XpsDocument。
- 新增 `WindowPdfViewer`（WebView2）：窗口显示后初始化并加载 PDF，失败记录日志并提示。
- `UserDoc_Click`：Win10+ 优先找 `Docs/*.pdf` 用 WebView2 打开，Win7 优先找 `Docs/*.xps` 用 DocumentViewer 打开；首选格式缺失时回退另一格式，均缺失时提示。
- csproj：Docs 下 `*.pdf`/`*.xps` 通配符 None 项随构建复制到输出目录（原 PDF 条目未复制导致运行期找不到文件）；新增 OsVersionHelper、WindowPdfViewer 编译项。
- 更新使用说明书帮助菜单说明。

#### 涉及文件
- ShaoLu/Utils/OsVersionHelper.cs（新增）、NativeMethods.cs
- ShaoLu/Views/WindowDocumentViewer.xaml / .xaml.cs
- ShaoLu/Views/WindowPdfViewer.xaml / .xaml.cs（新增）
- ShaoLu/MainWindow.xaml.cs
- ShaoLu/App.xaml.cs
- ShaoLu/ShaoLu.csproj
- 使用说明书.md

#### 构建验证结果
- 主项目（ShaoLu.csproj, Debug|x64）编译通过，无错误；Docs 下两份文档已复制到输出目录。

## 2026-08-24

### 类型：改进（WebView2 回退、示例保护、获取输入多点/相对定位）与修复（最近文件菜单）

#### 需求描述与修改内容说明
1. 系统未安装 WebView2 时，使用文档切换到 XPS/DocumentViewer 模式：`OsVersionHelper` 新增 `IsWebView2Installed`（CoreWebView2Environment.GetAvailableBrowserVersionString 探测）；`UserDoc_Click` 仅在 Win10+ 且已装 WebView2 时用 PDF，否则回退 XPS；启动日志记录 WebView2 状态。
2. 示例文件只允许另存为：`Save_Click` 检测当前文件位于程序目录 `Examples` 下时强制转另存为，不覆盖示例。
3. 获取输入步骤（ScreenText 模式）重构：新增 `TextPointItem` 模型与 `TextPoints` 列表，支持多个获取点按顺序获取文本、结果以换行合并；定位方式支持屏幕绝对坐标与图像相对定位（同点击图像步骤：选图+图片编辑窗口裁剪+点击点，运行时先匹配图像再按偏移读取，未匹配记失败）；旧版单点 `TextPoint` 首次访问时自动迁移；裁剪图随 .autostep 打包（StepsFile 新增 GetInputStep 分支）；详情模板改为获取点列表（定位方式下拉+坐标点选/选图编辑/删除）。
4. 修复“最近使用的文件”菜单标题为空无法展开：菜单无键 `{lex:Loc}` 按 `x:Name_Header` 解析而实际键为 `RecentFiles`，改为显式键 `{lex:Loc RecentFiles}`。

#### 涉及文件
- ShaoLu/Utils/OsVersionHelper.cs
- ShaoLu/Models/TextPointItem.cs（新增）
- ShaoLu/Viewmodels/GetInputStep.cs
- ShaoLu/Utils/StepsFile.cs
- ShaoLu/Converters/ConditionEnumToStringConverter.cs
- ShaoLu/Templates/StepDetailTemplates.xaml
- ShaoLu/MainWindow.xaml / .xaml.cs
- ShaoLu/App.xaml.cs
- ShaoLu/ShaoLu.csproj
- ShaoLu/Resources/Strings.resx、Strings.zh-CN.resx、Strings.zh-TW.resx、Strings.en-US.resx（新增 TextPointSource_Absolute/Relative、Input_TextPointsHint、Input_RelativeReady）
- NUnitTest/AutomationStepTests.cs（新增 TextPointItem/迁移/克隆用例）
- 使用说明书.md（获取输入、保存、使用文档章节）

#### 构建验证结果
- 主项目（ShaoLu.csproj, Debug|x64）编译通过，无错误。
- NUnit 测试全部通过：180 / 180。

### 类型：改进（保存可选打包原图）与修复（最近文件子菜单）

#### 需求描述与修改内容说明
1. 保存时可选择是否打包原图：`SaveAsAutoStepPackage` 新增 `includeSourceImages` 参数，将图像类步骤与获取输入相对定位点的原图以约定名 `images/{Uid|Id}_src{扩展名}` 写入包内；保存/另存为前弹窗询问（YesNo）；加载时原图路径失效自动回退到包内原图（`ImageRecognitionBase.ImgSrc` 与 `TextPointItem.ResolveSourceImagePath`），保证其他电脑上可重新编辑。
2. 修复“最近打开的文件”子菜单点不开：记录功能正常（recent_files.json 已有数据），原因是无子项的 MenuItem 点击不会触发 `SubmenuOpened`；改为主窗口启动时预构建菜单（无记录时显示禁用的“（无）”占位保证可展开），展开时再刷新列表。
3. resx 四语言新增 `SaveWithSourceImages` 键；更新使用说明书（保存/打开章节）。

#### 涉及文件
- ShaoLu/Utils/StepsFile.cs
- ShaoLu/Viewmodels/ImageRecognition.cs
- ShaoLu/Models/TextPointItem.cs
- ShaoLu/Viewmodels/GetInputStep.cs
- ShaoLu/MainWindow.xaml.cs
- ShaoLu/Resources/Strings.resx、Strings.zh-CN.resx、Strings.zh-TW.resx、Strings.en-US.resx
- 使用说明书.md

#### 构建验证结果
- 主项目（ShaoLu.csproj, Debug|x64）编译通过，无错误。
- NUnit 测试全部通过：180 / 180。

## 2026-08-28

### 类型：修复（测试获取文本卡死）

#### 问题现象
获取输入步骤使用“读取界面文本”方式时，点击“测试获取文本”后主界面无响应；`ReadTextAtPoint` 已正常返回，但 `Task.Run` 完成后界面仍卡死。

#### 原因与修复
- 原因：`TestOCR` 在 UI 线程上用 `ReadScreenTextsAsync(...).GetAwaiter().GetResult()` 同步阻塞等待；方法内部 `await Task.Run` 的延续捕获了 WPF 调度器，需要回到被阻塞的 UI 线程才能继续，形成经典 sync-over-async 死锁。
- 修复：`TestOCR` 改为 `async void`，直接 `await ReadScreenTextsAsync(...)`，延续正常回调度器后更新结果。排查确认其余 `GetAwaiter().GetResult()` 均在后台/启动路径，无同类风险。
- 排查技巧：可结合运行日志定位；`_logger.Error` 中异常未出现即可确认是死锁而非抛异常。

#### 涉及文件
- ShaoLu/Viewmodels/GetInputStep.cs

#### 构建验证结果
- 主项目（ShaoLu.csproj, Debug|x64）编译通过，无错误。
- NUnit 测试全部通过：180 / 180。
