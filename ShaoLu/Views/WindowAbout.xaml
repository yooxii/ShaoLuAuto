<Window x:Class="ShaoLu.Views.WindowAbout"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:lex="http://wpflocalizeextension.codeplex.com"
        xmlns:local="clr-namespace:ShaoLu.Views"
        mc:Ignorable="d"
        Title="WindowAbout" Height="350" Width="600">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="1" Grid.Column="1" >
            <TextBlock Text="{lex:Loc AppTitle}" TextAlignment="Center" FontSize="20" FontWeight="Bold"/>
            <TextBlock Text=""/>
            <TextBlock Text="{lex:Loc AboutContent}" TextAlignment="Center" FontSize="16"/>
            <TextBox Text="{lex:Loc Email}" TextAlignment="Center" FontSize="16" BorderThickness="0" IsReadOnly="True"/>
        </StackPanel>
    </Grid>
</Window>
