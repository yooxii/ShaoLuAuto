---
kind: external_dependency
name: FreeSql ORM (SQLite)
slug: freesql
category: external_dependency
category_hints:
    - vendor_identity
scope:
    - '**'
---

### 身份与角色

### 集成要点
- 使用 FreeSql ORM 框架进行数据访问。
- 本地 SQLite 文件存储，无需外部数据库服务。

### 使用约束
- 仅支持 SQLite 后端。
- 验证具体 API 调用方式请参考官方文档。